# AGENTS.md

Read **`CLAUDE.md`** first. It is the canonical repo guidance for Codex and other
coding agents.

This repo is the Omnivert app. The old outer workspace may still contain the
local `.venv`, but the GitHub repo root is this `omnivert-app/` directory.

